# Claude Boilerplate Repo

The boilerplate's own working copy: the harness in `.claude/` and
`.devcontainer/` is the product. `.boilerplate-dev` at the root marks this
checkout so detach nudges never fire here.

## Harness settings

Per-project choices the synced `.claude/CLAUDE.md` defers to. Where a line
here contradicts that file, this file wins.

- Harness: committed
- STATUS.md: private
- Commit policy: milestones
- Testing policy: on-request
- Commit session links: off
- Review page: off

## Current Status

See `_planning/STATUS.md` for current work and `CHANGELOG.md` for completed
milestones.
